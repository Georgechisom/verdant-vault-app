// Placeholder page — original project referenced scaffold components that are not
// present in this repo. Keep a minimal component to avoid build-time errors.
const Home = () => {
  return (
    <div className="flex items-center justify-center min-h-40 p-8">
      <div className="text-center text-muted-foreground">
        UI components missing — placeholder
      </div>
    </div>
  );
};

export default Home;
